/** BOINC RPC error code: operation still in progress */
export const BOINC_ERROR_IN_PROGRESS = -204;

/** Max polling attempts when waiting for async RPC operations */
export const MAX_ATTACH_POLL_ATTEMPTS = 30;

/** Delay between polling attempts for async RPC operations (ms) */
export const ATTACH_POLL_DELAY_MS = 1000;
